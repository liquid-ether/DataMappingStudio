namespace App.Domain;

/// <summary>
/// Marker type for the Domain assembly (entities, column-catalog model, rule AST, lineage model).
/// Real domain types are added in Phase 1. Zero external dependencies by design.
/// </summary>
public static class AssemblyMarker
{
}
