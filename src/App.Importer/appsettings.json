{
  "$comment": "Importer configuration. Override any value with a DMS_Importer__<Key> environment variable, or pass explicit command-line arguments (which take precedence). Paths may use environment variables like %LOCALAPPDATA%. The defaults target the same local working copy the desktop/web app uses, so 'import-excel <workbook.xlsx>' with no other arguments imports into the app's database.",
  "Importer": {
    "DbPath": "%LOCALAPPDATA%\\MappingStudio\\local.db",
    "MappingPath": "import-mapping.json",
    "RemoteFolder": "%LOCALAPPDATA%\\MappingStudio\\remote",
    "ChangedBy": "import"
  }
}
