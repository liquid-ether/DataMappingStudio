@page "/admin/providers"
@attribute [Authorize(Policy = Permissions.SecurityConfigure)]

<AdminGuard Permission="@Permissions.SecurityConfigure">
    <AdminNav />
    <ProvidersAdmin />
</AdminGuard>
