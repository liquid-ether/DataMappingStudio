{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_Auth": "Set Auth:Require=true (or env Auth__Require=true) in production to require an authenticated Windows user (Negotiate) for every endpoint. Off by default so local dev and the E2E suite run without credentials.",
  "Auth": {
    "Require": false
  }
}
